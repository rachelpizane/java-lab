package com.rachelpzm.infnet.metodos;

public class TestaMetodo { // Treinando como declarar metodo.
    // Metodo publico e dinamico para calcular a soma de dois numeros reais.
    public double somarNumerosReais(double a, double b){
        return 0;
    }

    // Metodo privado e estático que inverta a ordem dos caracteres de uma frase
    private static String inverterOrdemDosCaracteresDaFrase(String frase){
        return frase;
    }

    // Metodo de pacote que verifique se um servidor está ativo ou não.
    boolean verificarServidorAtivo(String ip){
        return true;
    }

    // Metodo publico e estático que verifique se um CNPJ é valido.
    public static boolean verificarValidezCNPJ(String cnpj){
        return false;
    }
}
